import React from 'react'

function UserList() {
  return (
    /* a component that renders a list of users in the database  */
    /* only works in the profile page  */
    /* the navbar placeholder changes to search users */
    /* then displays this component */
    /* when a particular user profile is clicked it redirects to the profile page which is updated  to remove the ability to  delete photos */
    <div>UserList</div>
  )
}

export default UserList